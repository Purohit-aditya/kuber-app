apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-demo-app.fullname" . }}-config
  labels:
    {{- include "k8s-demo-app.labels" . | nindent 4 }}
data:
  APP_VERSION: {{ .Values.configMap.appVersion | quote }}
