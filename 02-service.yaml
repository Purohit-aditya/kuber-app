apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-app
  labels:
    app: k8s-demo-app
spec:
  selector:
    app: k8s-demo-app
  type: NodePort
  ports:
    - name: http     
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 31700
  